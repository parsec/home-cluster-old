---
version: "3"

tasks:
  sync:
    desc: Sync flux-system with the Git Repository
    cmds:
      - flux reconcile source git flux-system
    silent: true

  reconcile:all:
    desc: Reconcile all core Kustomizations
    cmd:
      - flux reconcile crds flux-system core apps
    silent: true
