---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins-config
  namespace: jenkins
spec:
  storageClassName: managed-nfs-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
